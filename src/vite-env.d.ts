/// <reference types="vite/client" />

// Versión con la que se compiló este bundle. Inyectada por Vite (define) en vite.config.ts.
declare const __APP_VERSION__: string
/** Commit con el que se construyó: la versión que se reporta al panel de errores. */
declare const __COMMIT__: string
